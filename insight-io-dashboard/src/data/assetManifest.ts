import type { AssetManifest } from '../types';

export const assetManifest: AssetManifest = {
  video: '/assets/camera-feed.mp4',
  pointCloud: '/assets/map-cloud.pcd',
  referenceFrame: '/assets/reference-frame.png',
  telemetry: '/assets/telemetry.json',
  missionEvents: '/assets/mission-events.json',
};
